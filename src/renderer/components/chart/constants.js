export const SVG_SIZE = 1000;
export const MARGIN = 50;
export const VIEWBOX_SIZE = SVG_SIZE + 2 * MARGIN;
export const CENTER = VIEWBOX_SIZE / 2;
export const OUTER_RADIUS = SVG_SIZE * 0.40;
export const INNER_RADIUS = OUTER_RADIUS * 0.3;
export const ROWS = 6;